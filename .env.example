# Pinecone — MA law vector index (clause-ma-laws-3072, namespace __default__)
PINECONE_API_KEY=
PINECONE_INDEX=clause-ma-laws-3072
PINECONE_NAMESPACE=__default__

# Gemini — embeddings (gemini-embedding-001) + legal reasoning (gemini-2.5-flash)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# Set to "false" to force local statute fallback (useful for offline tests)
MEDIUS_USE_PINECONE=auto

# Optional — Anthropic for translation/OCR
ANTHROPIC_API_KEY=
